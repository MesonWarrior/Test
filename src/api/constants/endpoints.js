const tree = 'api.user.tree';

const node = `${tree}.node`;

export const getTree = `${tree}.get`;

export const createNode = `${node}.create`;
export const renameNode = `${node}.rename`;
export const deleteNode = `${node}.delete`;
